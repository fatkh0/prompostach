import React from "react";
import MainPage from "./MainPage";

type TProps = {}

const MainPageContainer: React.FC<TProps> = React.memo(() => {

    return <MainPage />
})

export default MainPageContainer